# یادداشت کلود درباره‌ی پروژه‌ی پلاسکو گلی

این فایل خلاصه‌ی چیزهاییه که کلود درباره‌ی این پروژه به‌خاطر سپرده.
داخل پوشه‌ی پروژه گذاشته شده تا خودت هم بتونی ببینیش. آخرین به‌روزرسانی: ۱۴۰۵/۰۵/۱۰ (2026-08-01) — نسخه‌ی ۱۱.

## ⚡ تازه‌ترین کار — ۱۴۰۵/۰۵/۱۰ (کشِ بایتِ فشرده + یک باگِ کهنه که پیدا شد)

دو تغییر. دومی باگی است که **از قبل وجود داشت** و سرِ اولی پیدا شد.

**۱) پاسخِ فشرده حالا کش می‌شود.** فهرستِ محصولات برای همه‌ی بازدیدکننده‌ها
بایت‌به‌بایت یکسان است، ولی تا امروز برای هر درخواست از نو `JSON.stringify` و
بعد brotli می‌شد: ۰٫۱۵۵ میلی‌ثانیه روی صفحه‌ی ۲۴تایی و ۰٫۶۱ روی فهرستِ کامل.
چون موتورِ ما همگام است، آن زمان **کلِ سایت** را قفل می‌کند نه فقط همان درخواست
را؛ با ۱۰۰ بازدیدکننده‌ی هم‌زمان یعنی ۱۶ تا ۶۱ میلی‌ثانیه قفل. حالا بایتِ فشرده
نگه داشته می‌شود و همان کار ۸۰۰۰ برابر ارزان‌تر شد.

کلیدِ کش **ETag** است نه آدرس، چون ETag را خودِ روت از امضای کاتالوگ می‌سازد؛
پس باطل‌سازی خودکار است و هیچ «پاک‌کردنِ دستی» نمانده که یادش برود. سقفش ۴
مگابایت است و قدیمی‌ترین ورودی بیرون می‌رود.

**دو شرط برای کش‌شدن، و شرطِ دوم است که امنیت را تضمین می‌کند:** پاسخ باید ETag
داشته باشد **و** `Cache-Control` بگوید `public`. اول نوشته بودم شرطِ اول کافی
است چون ETagِ خودکارِ Express بعد از میان‌افزارِ ما ساخته می‌شود — امروز درست
است، ولی یک جزئیاتِ پیاده‌سازیِ Express است و تکیه بر آن یعنی داده‌ی مشتری یک
ارتقای Express با نشت فاصله دارد. با پاسخِ جعلیِ `no-store + ETag` و
`private + ETag` مستقیم آزمودم: هر دو رد می‌شوند.

**۲) باگ: دو ویرایش در یک ثانیه، ویرایشِ دوم گم می‌شد.**
امضای کاتالوگ از `MAX(updated_at)` می‌آمد و دقتِ `datetime('now')` **یک ثانیه**
است. دو ویرایشِ محصول در یک ثانیه — دقیقاً کاری که «ذخیره‌ی سریعِ جدول» در پنل
می‌کند — تعداد و مجموعِ موجودی را هم عوض نمی‌کنند، پس امضا **یکسان** درمی‌آمد و
ویرایشِ دوم نامرئی می‌شد. نه ۳۰ ثانیه: **تا تغییرِ بعدیِ کاتالوگ.** هم کشِ
حافظه‌ی سرور و هم ETagِ مرورگر را می‌گرفت، یعنی یک اصلاحِ قیمت می‌توانست
مدت‌ها از چشمِ مشتری پنهان بماند.

با شمارنده‌ی یکنواختِ `catalog_rev` بسته شد که **سه تریگرِ SQLite** نگهش
می‌دارند (درج/ویرایش/حذف). تریگر و نه بالابردنِ شمارنده در جاوااسکریپت، چون روی
`products` از جاهای زیادی نوشته می‌شود — ویرایش، ساخت، حذف، انتشار، رزرو و
برگشتِ موجودی، ذخیره‌ی گروهی، ایمپورت، ابزار seed — و اگر یک جا فراموش شود باگ
بی‌صدا برمی‌گردد. از تریگر هیچ مسیری فرار نمی‌کند، حتی `sqlite3` با دست.

شرطِ `published = 1` روی تریگرها عمدی است و همان قاعده‌ی قبلی را نگه می‌دارد:
ویرایشِ **پیش‌نویس** نباید کشِ مشتری را باطل کند. تو همین حالا ۸۸ پیش‌نویس را
برای گذاشتنِ عکس ویرایش می‌کنی؛ بدون این شرط هر ذخیره‌ی تو کشِ همه‌ی
بازدیدکننده‌ها را دور می‌ریخت. ولی *منتشر کردن* و *پنهان کردن* فوراً باطل می‌کند.

هزینه: ۰٫۰۴۷ میلی‌ثانیه در هر **نوشتن** (نه در بازدید)؛ خواندنِ امضا ۰٫۰۰۹ ماند.

**چطور ثابت شد:** با سرورِ واقعی و تغییرِ قیمت از **مسیرِ خودِ پنل** (نه اتصالِ
کناری به فایل): ETag فوراً عوض شد، بایتِ تازه آمد، ETagِ کهنه دیگر ۳۰۴ نگرفت و
ETagِ جاری همچنان گرفت. ۱۵ تستِ تازه (V32) نگهبانِ همین‌هاست.

> درسی که ماند: بارِ اول این را با اتصالِ **دومی** به فایلِ دیتابیس تست کردم و
> امضا عوض نشد. اولش فکر کردم کشِ خودم باگ دارد؛ نداشت — تستم `updated_at` را
> جلو نمی‌برد. ولی همان مسیرِ غلط باگِ واقعیِ ثانیه را لو داد. تستِ مشکوک را
> دور نریز، اول بفهم چرا قرمز است.

## نسخه‌ی ۱۰ — ۱۴۰۵/۰۵/۱۰ (کاتالوگ ۱۰۰ محصول + صفحه‌ی فهرست)

**کاتالوگ ۱۰۰ محصول شد: ۱۲ واقعی + ۸۸ پیش‌نویس.**
۸۸ محصولِ تازه با `node tools/seed-catalog.js` ساخته شدند و همه `published = 0`
هستند، یعنی **هیچ‌کدام روی سایت دیده نمی‌شوند**. دلیلش: محصولِ بی‌عکس با قیمتِ
حدسی، هم اعتمادِ مشتری را خراب می‌کند و هم ۸۸ صفحه‌ی خالی به گوگل می‌دهد.
هر کدام `import_batch = 'seed-100'` دارند، پس کلِ این دسته با یک دستور
(`node tools/seed-catalog.js --rollback`) برمی‌گردد. اگر محصولی از این دسته
در سفارشی آمده باشد، rollback کلاً انجام نمی‌شود.

**کارِ تو از اینجا:** در پنل → انبار و کالا، فیلترِ «بدون عکس» را بزن، برای هر
کالا عکس بگذار و نام/قیمت را درست کن، بعد دکمه‌ی چشم را بزن تا روی سایت بیاید.
انتشارِ کالای بی‌عکس عمداً تأییدِ جداگانه می‌خواهد (هم تکی، هم گروهی) و این
شرط در **سرور** است نه فقط در مرورگر.

**صفحه‌ی تازه: `products.html` — فهرستِ کاملِ محصولات.**
صفحه‌ی اصلی دست‌نخورده ماند. صفحه‌ی جدید ستونِ فیلتر دارد (دسته، بازه‌ی قیمت،
فقط موجود)، مرتب‌سازی، و صفحه‌بندیِ شماره‌دار — ۲۴ کالا در هر صفحه.
همه‌ی حالت در آدرس است، پس لینک قابلِ اشتراک است و دکمه‌ی برگشتِ مرورگر کار
می‌کند. با ۱۰۰ محصول تست شد: ۵ صفحه، بدون هم‌پوشانی.

**دو نشتی که پیدا و بسته شد** (هر دو تست دائمی گرفتند):
1. `/api/shop/categories` که منوی هدر از آن ساخته می‌شود، **پیش‌نویس‌ها را هم
   می‌شمرد**: منو می‌گفت «ظروف نگهداری ۲۱» ولی صفحه ۳ تا نشان می‌داد. بدتر
   اینکه همین عدد وجودِ ۱۸ محصولِ منتشرنشده را لو می‌داد. حالا `count` فقط
   منتشرشده‌هاست و `countAll` (که پیش‌نویس را هم دارد) فقط به پنل می‌رود.
2. دسته‌های آشغالِ «Test» و «تست» به همه‌ی بازدیدکننده‌ها نشان داده می‌شدند و
   به صفحه‌ی خالی می‌رسیدند. هم از دیتابیس حذف شدند، هم دسته‌ی بی‌کالا دیگر
   در مسیرِ عمومی نمی‌آید.

**EXIF عکس‌ها حالا پاک می‌شود** (`lib/image-clean.js`). عکسی که با گوشی از
کالا می‌گیری، **مختصات جغرافیایی مغازه** را داخل خودش دارد؛ یعنی از دلِ عکسِ
یک سطلِ پلاستیکی می‌شد آدرسِ مغازه را درآورد. مدل گوشی و گاهی مسیرِ فایل روی
کامپیوتر هم داخلش هست. حالا قبل از نوشتن روی دیسک پاک می‌شود. پیکسل‌ها اصلاً
لمس نمی‌شوند (هیچ افتِ کیفیتی ندارد). اگر عکس تگِ چرخش داشته باشد عمداً
دست‌نخورده می‌ماند، چون حذفش عکس را خوابیده نشان می‌دهد.

**تعداد تست: ۷۱۹** (۶۴۱ دود + ۴۱ امنیت + ۳۷ تخفیف) — همه سبز.

---

## نسخه‌ی ۹ — ۱۴۰۵/۰۵/۰۹ (بازبینی وضعیت و رفع اشکال)

**پروژه حالا زیر گیت است.** `git init` + `.gitignore`. از این به بعد برگرداندن هر
تغییر با گیت انجام می‌شود، نه با کپی‌کردنِ کلِ پوشه. سه چیز عمداً بیرونِ گیت ماندند و
**هرگز نباید وارد شوند**: `.env` (کلید درگاه و پیامک و SESSION_SECRET)، فایل‌های
`backend/data/*.db` (شماره‌ی موبایل و آدرسِ واقعیِ مشتری‌ها — پاک‌کردنِ اینها از
تاریخچه‌ی گیت بعداً تقریباً غیرممکن است)، و `node_modules` و `logs` و `backup/`.
پوشه‌ی `backup/` سرجایش روی دیسک هست، فقط دیگر وارد تاریخچه نمی‌شود.

**باگِ ارسال رایگان درست شد.** `free_shipping_over` روی **۹۰,۰۰۰,۰۰۰** مانده بود —
همان عددی که `test-smoke.js` خط ۴۵۷ موقع تست می‌نویسد. در `admin_log` ردیف ۱۹۱۲ مورخ
۱۴۰۵/۰۵/۰۸ ساعت ۰۸:۳۶ ثبت شده بود، یعنی از اجرای تستِ **قبل از** ساختِ دیوارِ سندباکس
جا مانده و برنگشته بود. اثرش این بود که نوار سبد به مشتری می‌گفت «فقط ۸۹,۷۴۲,۵۰۰
تومان دیگر تا ارسال رایگان!». حالا طبق تصمیمِ صاحب مغازه: هزینه‌ی ارسال **۱۵۰,۰۰۰**
تومان و سقفِ ارسال رایگان **۳,۰۰۰,۰۰۰** تومان. با خودِ `getShippingQuote()` روی شش
مبلغِ مرزی راستی‌آزمایی شد. بکاپِ قبل از تغییر: `data/backups/manual-before-shipping-fix-*.db`.

**آپلودِ تست دیگر به پوشه‌ی عکسِ واقعی دست نمی‌زند** (`PG_PICTURE_DIR`) — پایین‌تر
مفصل توضیح داده شده.

**چیزهایی که عمداً دست‌نخورده ماندند** (تصمیمِ صاحب مغازه):
- داده‌ی تستی داخل دیتابیس (۲۷۲ سفارش و ۱۴۰ کاربرِ `0912…`) فعلاً می‌ماند تا داشبورد
  پر باشد. قبل از افتتاحِ واقعی با `node tidy-test-data.js` پاک شود.
- موجودیِ «سطل شیاردار درب چوبی ۳ لیتر» صفر است و **واقعاً ناموجود است**، پس درست است.

**آنچه هنوز مانده و کد نمی‌تواند حلش کند:** هشت محصول (شماره‌ی ۵ تا ۱۲) عکسِ واقعی
ندارند و فقط آیکون نشان داده می‌شوند. جایگزینِ آیکون همه‌جا درست کار می‌کند (کارت،
نمای سریع، صفحه‌ی محصول، پیشنهادِ جست‌وجو، و `og:image` وقتی عکس نیست اصلاً گذاشته
نمی‌شود) پس سایت شکسته به‌نظر نمی‌رسد — ولی این هشت قلم باید با دوربین عکس گرفته شوند.

---

## این سایت چیه؟
فروشگاه آنلاین واقعیِ مغازه‌ی خانوادگی «پلاسکو گلی» (ساری، بلوار کشاورز، تلفن ۰۹۱۱۳۵۶۷۴۰۹).
سایت فارسی و راست‌چین، با تم تیره‌ی لوکس (سبز-مشکی + فیروزه‌ای + طلایی).

## با چی ساخته شده؟
- ظاهر سایت: HTML/CSS/JS ساده (بدون فریم‌ورک) — ۱۴ صفحه + css/style.css + js جدا برای هر صفحه
- سرور: Node.js + Express (نیاز به Node نسخه‌ی ۲۲.۵ به بالا)
- دیتابیس: SQLite داخلی Node (فایل backend/data/polasco.db + بکاپ روزانه‌ی خودکار)
- ورود کاربر: با کد پیامکی یا رمز عبور اختیاری (scrypt)
- پرداخت: درگاه زرین‌پال (فعلاً حالت آزمایشی)
- تصمیم مهم: Next.js و Prisma بررسی و رد شدند — برای این فروشگاه فایده‌ی واقعی نداشتند
  و بخش حساس موجودی انبار (اتمی و ضد فروش بیش از انبار) نباید ریسک بازنویسی بخوره.

## قابلیت‌ها — نسخه به نسخه
- نسخه‌ی ۲: تم لوکس تیره، هدر دوطبقه، جستجوی زنده، سئوی کامل
- نسخه‌ی ۳: فیلتر قیمت + مرتب‌سازی، علاقه‌مندی‌ها، پروفایل (اسم)، نوار پایین موبایل، مودال خوش‌آمد
- نسخه‌ی ۴: پنل مدیریت + وضعیت سفارش + سخت‌سازی امنیتی
- نسخه‌ی ۵: رمز عبور اختیاری، پروفایل و آدرس‌ها، تب مشتری‌ها در پنل، سئوی 410، صفحه‌بندی
- نسخه‌ی ۶: بک‌اند ۱۰ لول (ETag/WAL/ایندکس)، ورود تمام‌صفحه، منوی مرتب‌سازی سفارشی، تجربه‌ی خرید بهتر
- نسخه‌ی ۷: هزینه ارسال/ارسال رایگان + لغو و مرجوعی و کد رهگیری + نظرات با تأیید + گالری و مشخصات
- نسخه‌ی ۸: کد تخفیف + دسته‌بندی پویا + اطلاعیه/تعطیلی + قوانین و ۴۰۴ + سئوی سرور-ساید + آماده‌ی دیپلوی — ۱۰۸ تست
- نسخه‌ی ۹: پیگیری سفارش مهمان + فاکتور چاپی + خروجی CSV + سخت‌سازی ورود مدیر + دسترس‌پذیری WCAG AA + WebP
- نسخه‌ی ۱۰: کاتالوگ ۱۰۰ محصول + صفحه‌ی `products.html` با فیلتر و صفحه‌بندی + پاک‌سازی EXIF عکس + بستنِ نشتیِ شمارشِ پیش‌نویس — ۷۰۴ تست
- نسخه‌ی ۱۱ (این نسخه): **کشِ بایتِ فشرده‌ی JSON (۸۰۰۰× ارزان‌تر) + بستنِ باگِ «امضای یکسان در یک ثانیه» با شمارنده‌ی `catalog_rev` — ۷۱۹ تست**

## نسخه‌ی ۱۱ — جزئیات فنی (۱۴۰۵/۰۵/۱۰)

**`lib/static-compress.js` — کشِ بایتِ فشرده‌ی JSON.**
`compressJson` قبلاً بی‌حالت بود: هر درخواست `JSON.stringify` + brotli. حالا
نتیجه در `jsonCache` می‌نشیند با کلیدِ `` `${etag}|${encoding}` ``.
- سقف بر مبنای **بایت** است (۴MB) نه تعداد ورودی. اول «۱۲۰ ورودی» گذاشته بودم؛
  با ۱۰۰۰ محصول هر پاسخ ~۶۶KB می‌شود و آن سقف بی‌صدا تبدیل به ۸ مگابایت می‌شد.
- `cacheableJson(res)` دو شرط را می‌سنجد: وجودِ ETag، و `public` بودنِ
  `Cache-Control` (و نبودِ `no-store`/`private`). `server.js` برای کلِ `/api`
  پیش‌فرض `no-store` گذاشته، پس مسیرهای شخصی خودبه‌خود رد می‌شوند.
- `jsonCacheStats()` فقط برای تست است تا بشود **ثابت** کرد بارِ دوم از کش آمد.

**`lib/db.js` — شمارنده‌ی `catalog_rev` و سه تریگر.**
- `trg_catalog_rev_ins` / `_upd` / `_del` روی جدولِ `products`.
- `_upd` شرطش `new.published = 1 OR old.published = 1` است: هر دو طرفِ OR لازم
  است، وگرنه انتشار یا پنهان‌کردن یکی از دو حالت را از دست می‌داد.
- تریگرها در همان بلوکِ اسکیما با `CREATE TRIGGER IF NOT EXISTS` نوشته شده‌اند و
  آن بلوک در هر بالا‌آمدن اجرا می‌شود، پس روی دیتابیسِ **موجود** هم خودکار
  نصب شدند. لازم نبود مهاجرتِ دستی بنویسم.
- `catalog_rev` در `SETTING_DEFAULTS` **نیست**، پس `setSettingsTx` نمی‌تواند
  رویش بنویسد — همان محافظتی که `reviews_rev` دارد.
- امضا حالا `n.m.s.rN.cM` است و جزءِ `c` تنها جزئی است که *تضمین* می‌کند امضا با
  هر نوشتنِ دیده‌شدنی عوض می‌شود. `n` و `m` و `s` را نگه داشتم چون ارزان‌اند و
  امضا را برای آدم خواناتر می‌کنند.

**اگر روزی کش داده‌ی کهنه داد:** اول `catalog_rev` را نگاه کن، نه کش را. کش
حافظه‌ی مستقلی از امضا ندارد؛ هر کهنگی یعنی امضا عوض نشده.

## نسخه‌ی ۱۰ — جزئیات فنی (۱۴۰۵/۰۵/۱۰)

**`products.html` + `js/products.js` — فهرستِ کامل.**
- حالت کاملاً در آدرس است: `?cat=…&min=…&max=…&inStock=1&sort=…&page=…`.
  دلیلش این است که لینک قابلِ اشتراک باشد و دکمه‌ی back مرورگر واقعاً کار کند؛
  اگر حالت در حافظه‌ی جاوااسکریپت بود، مشتری بعد از دیدنِ یک کالا و برگشتن،
  فیلترهایش را از دست می‌داد.
- هر درخواست یک شماره‌ی ترتیبی (`reqSeq`) دارد و پاسخِ کهنه دور ریخته می‌شود؛
  وگرنه اگر کاربر سریع دو فیلتر بزند، ممکن است نتیجه‌ی فیلترِ اول دیرتر برسد و
  روی نتیجه‌ی دوم بنشیند.
- زیر ۹۶۰ پیکسل ستونِ فیلتر به کشوی کناری تبدیل می‌شود (نه آکاردئونِ بالای صفحه)،
  چون در موبایل کاربر باید هم‌زمان فیلتر را ببیند و هم بداند کجاست.
- اسکلتِ لودینگ (`.pl-skel-*`) در `prefers-reduced-motion` خاموش می‌شود.

**سئو با ۱۰۰ محصول فرق می‌کند.**
- `<link rel="canonical">` پویا نوشته می‌شود، و ترکیب‌های عمیقِ فیلتر
  (قیمت/موجودی/مرتب‌سازی) `noindex, follow` می‌گیرند. دلیل: هر ترکیب یک آدرسِ
  تقریباً تکراری می‌سازد و بودجه‌ی خزشِ گوگل را می‌خورد بدون اینکه چیزی اضافه کند.
- سایت‌مپِ سرور فقط `/products.html` و `?cat=` را دارد — هیچ فیلترِ دیگری.
  این با `noindex` سمتِ فرانت باید همیشه هم‌راستا بماند؛ اگر یکی را عوض کردی
  آن یکی را هم عوض کن.

**یک کنترل، یک رفتار.** قبلاً دکمه‌های دسته‌بندی دو رفتار داشتند: در صفحه‌ی اصلی
نوارِ محصولات را فیلتر می‌کردند، جای دیگر ناوبری می‌کردند. با ۱۰۰ محصول این
غلط شد، چون نوارِ صفحه‌ی اصلی فقط بخشی از دسته را نشان می‌داد و کاربر فکر می‌کرد
همه‌اش همین است. حالا **همه‌ی** لینک‌های دسته (منوی هدر، کشوی موبایل، کارت‌های
صفحه‌ی اصلی، فوتر، بردکرامب) به `/products.html?cat=…` می‌روند؛ فقط قرص‌های
بالای نوارِ صفحه‌ی اصلی هنوز همان نوار را فیلتر می‌کنند. از صفحه‌ی ۲ به بعدِ
«بیشتر ببین» یک لینکِ «همه را با فیلتر ببین» ظاهر می‌شود.
اگر روزی `products.js` روی صفحه‌ای لود باشد، تابعِ `window.PL_setCat` وجود دارد و
منو به‌جای ناوبری همان‌جا فیلتر می‌کند.

**`tools/seed-catalog.js`.** ۸۸ کالا با `published = 0` و `import_batch = 'seed-100'`.
برگشت: `node tools/seed-catalog.js --rollback` — ولی اگر کالایی از این دسته در
سفارشی آمده باشد، rollback کلاً انجام نمی‌شود (نه اینکه بخشی را پاک کند).

## نسخه‌ی ۹ — جزئیات فنی (۱۴۰۵/۰۵/۰۷ و ۰۸)
یک دورِ بلندِ خودگردان بود: کلود خودش تسک تعریف کرد و پشت‌سرهم جلو برد.
برگشت اضطراری کلِ این نسخه: `backup/pre-v9-20260729-045749/`

**پیگیری سفارش بدون ورود** (`POST /api/orders/track`) — مشتری با شماره‌ی سفارش + موبایل وضعیت را می‌بیند.
- بخش پیگیری در صفحه‌ی اصلی + لینکش در فوتر همه‌ی صفحه‌ها؛ خط زمانیِ مراحل در `main.js`
- نکته‌ی امنیتی: پیامِ «سفارشِ ناموجود» و «موبایلِ غلط» **عین هم** است. اگر فرق داشت، می‌شد با
  حدس‌زدن شماره‌ی سفارش فهمید کدام سفارش‌ها وجود دارند.
- پاسخ عمداً لاغر است: نام، پلاک، کدپستی، شناسه‌ی کاربر و کد بانک بیرون نمی‌آید

**فاکتور چاپی** — دکمه‌ی چاپ روی سفارش‌های پرداخت‌شده/ارسال‌شده/تحویل‌شده در صفحه‌ی حساب.
- `@media print` در انتهای style.css؛ فاکتور روی کاغذِ سفید چاپ می‌شود، نه تمِ تیره
- جمعِ کالاها از خودِ اقلام حساب می‌شود ولی **مبلغ قابل پرداخت همان `total` سرور است** — دو
  منبعِ حقیقت نداریم؛ اگر روزی جمع نخواند، عددِ سرور برنده است
- میزبانِ فاکتور با `aria-hidden` از صفحه‌خوان پنهان است (روی صفحه دیده نمی‌شود)

**خروجی CSV** (مشتری‌ها + انبار) در پنل — `GET /api/admin/export/customers` و `/export/inventory`.
- با BOM شروع می‌شود و خطوط CRLF است، وگرنه اکسلِ فارسیِ ویندوز فارسی را مربع نشان می‌دهد
- **ضدِ تزریق فرمول اکسل:** سلولی که با `=` یا `+` یا `-` یا `@` شروع شود با `'` پیشوند می‌گیرد؛
  وگرنه اسمِ مشتریِ دستکاری‌شده روی کامپیوترِ تو تبدیل به فرمولِ اجراشدنی می‌شد
- فیلترِ روی صفحه به خروجی منتقل می‌شود؛ شماره‌ی مدیر در فهرست مشتری‌ها نیست؛ پاسخ کش نمی‌شود
- ستون‌های تحلیلی: تعداد سفارش، مجموع خرید، آخرین خرید / و برای انبار: «کافی برای چند روز»

**سخت‌سازی ورود مدیر** (`lib/login-guard.js`)
- قفلِ **به‌ازای هر حساب** بعد از تلاش‌های ناموفق پیاپی؛ کد پاسخ `429` نه `401` تا کاربر بفهمد
  مشکل رمزش نیست. شماره‌ی ثبت‌نام‌نکرده هم قفل می‌شود، وگرنه تفاوتِ رفتار لو می‌داد کدام شماره
  در دیتابیس هست. ورود موفق شمارنده را صفر می‌کند.
- انقضای بی‌کاریِ پنل: ۳۰ دقیقه (`PANEL_IDLE_MS`)
- سطرهای `login_ok` / `login_failed` در دفتر رویدادها با شماره‌ی **ماسک‌شده** + IP + مرورگر.
  تلاشِ ناموفقِ مشتریِ عادی ثبت نمی‌شود (دفتر را پر می‌کرد بی‌آنکه چیزی بگوید).

**دسترس‌پذیری — کنتراست در سطح WCAG 2.2 AA** (`tools/contrast-audit.js`)
- ابزار خودش نوشته شد چون بدون اندازه‌گیری هر «درست کردنِ رنگ» حدس است. رنگ‌ها را از CSS
  می‌خواند، `var()`ها را باز می‌کند، شفافیت را روی زمینه ترکیب می‌کند و نسبت را حساب می‌کند.
- **محدودیتِ صادقانه‌ی ابزار:** آبشارِ CSS را بازپخش نمی‌کند، پس جدولِ `CONTEXTS` دارد که
  می‌گوید هر پیشوندِ کلاس روی چه زمینه‌ای می‌نشیند. سطحِ تازه اگر ثبت نشود، گزارش غلط می‌دهد.
- ۳ متغیرِ رنگیِ تازه: `--ink-dim` (متنِ کم‌نور، از ۳٫۳ به ۴٫۶)، `--line-control` (کادرِ
  اینپوت‌ها از ۱٫۶ به ۳٫۳ — عملاً نامرئی بود)، `--ink-on-warm` (متنِ تیره روی نشانِ تخفیف، از ۲٫۸ به ۶٫۵)
- **چرا نشان‌های تخفیف تیره‌متن شدند نه کم‌رنگ‌تر:** راهِ ساده‌تر تیره‌کردنِ خودِ نارنجی بود،
  ولی آن‌وقت همان نشان‌هایی که باید چشم را بگیرند کم‌جان می‌شدند. متنِ تیره روی رنگِ روشن همان
  کاری است که دکمه‌ی «خرید» از قبل می‌کرد، پس با زبانِ طراحی هم می‌خواند.
- تستِ V23 خودِ ابزار را هم می‌سنجد: عمداً یک رنگِ بد به CSS اضافه می‌کند و مطمئن می‌شود ابزار
  می‌گیردش. بدون این، ابزارِ خراب تا ابد «سبز» گزارش می‌داد.

**عکس‌ها — WebP و ابعاد صریح**
- `lib/webp-negotiate.js`: اگر `x.webp` کنارِ `x.jpg` باشد و مرورگر بپذیرد، همان تحویل می‌شود.
  **۶۶۰KB → ۲۹۵KB (۵۵٪ سبک‌تر)** بدون یک خط تغییر در HTML/JS. هدرِ `Vary: Accept` روی هر دو
  جواب می‌رود، وگرنه پروکسیِ میانی نسخه‌ی webp را به مرورگرِ قدیمی هم می‌دهد.
- `tools/optimize-images.js` نسخه‌های webp را می‌سازد (cwebp → magick → convert → ffmpeg → Pillow؛
  هر کدام بود). فایلِ اصلی **جایگزین نمی‌شود** — اگر webpها را پاک کنی سایت بی‌صدا به قبل برمی‌گردد.
- `lib/imagesize.js`: ابعاد را از خودِ بایت‌های فایل می‌خواند، بدون هیچ پکیج (JPEG/PNG/GIF/WebP،
  هر سه گونه‌ی VP8/VP8L/VP8X).
- آپلود حالا **ابعاد** را هم می‌سنجد نه فقط حجم: بزرگ‌تر از ۴۰۰۰ پیکسل → ۴۱۳، کوچک‌تر از ۸۰ → ۴۰۰.
  دلیلش: یک JPEG فشرده‌ی ۶۰۰۰×۶۰۰۰ راحت زیرِ سقفِ ۲ مگابایت جا می‌شود ولی روی گوشیِ مشتری
  حدود ۱۴۴ مگابایت حافظه برای رمزگشایی می‌خواهد.
- **چرا `srcset` نگذاشتیم:** حساب کردم. کادرِ عکسِ هیرو روی گوشیِ ۳۹۰ پیکسلی ۱۷۹×۳۰۴ است و عکس
  با `object-fit:cover` می‌نشیند؛ وقتی کادر از عکس بلندتر باشد **ارتفاع** تعیین می‌کند چقدر از
  عرضِ منبع لازم است (۳۷۵px، روی DPR۲ یعنی ۷۵۰). ولی مرورگر برای انتخاب از srcset فقط به عرضِ
  کادر (۱۷۹) نگاه می‌کند — پس نسخه‌ی ۴۸۰ را برمی‌داشت و عکسِ اصلیِ صفحه روی گوشی تار می‌شد.
  ساختِ نسخه‌ی کم‌عرض رفت پشتِ پرچمِ `--variants`.

**بندانگشتی‌ها — `?w=320`** (همان کاری که بالا برای هیرو *نکردیم*، ولی اینجا درست است)
- سه جای سایت عکس را در کادری نشان می‌دهد که اندازه‌اش در CSS **ثابت** است: پیشنهادِ جست‌وجو
  ۴۴px، ردیفِ سبد ۷۶px، فهرستِ کالای پنل ۴۰px. برای هر کدام همان فایلِ ۹۳۸ پیکسلی می‌رفت —
  یک جست‌وجو با ۶ پیشنهاد **۳۶۶KB** برای شش مربعِ ۴۴ پیکسلی، و فهرستِ پنل **۷۳۰KB**.
- **چرا اینجا برخلافِ هیرو مطمئنیم:** کادر واکنش‌گرا نیست، عددش در CSS نوشته شده. پس حدسی
  در کار نیست و مرورگر هم چیزی انتخاب نمی‌کند — خودِ ما آدرس را می‌سازیم.
- عدد ۳۲۰ از بزرگ‌ترین کادر آمد: ۷۶px روی صفحه‌ی DPR۳ یعنی ۲۲۸ پیکسلِ واقعی، و چون
  `object-fit:cover` است **ضلعِ کوچکِ** منبع باید ≥۲۲۸ باشد. عرضِ ۳۲۰ روی عکسِ افقیِ ۱٫۲۳
  یعنی ارتفاعِ ۲۵۹ — با حاشیه. **۲۹۵KB → ۶۴KB یعنی ۷۸٪ کمتر** (تکی: ۶۰KB → ۱۲KB).
- `PG.thumb(src)` در common.js آدرس را می‌سازد؛ فقط روی مسیرِ داخلیِ `/picture` و فقط اگر
  خودش پارامتر نداشته باشد. سرور در `lib/webp-negotiate.js` اول دنبالِ `x-320w.webp` می‌گردد
  و اگر نبود می‌افتد روی نسخه‌ی کامل.
- عرضِ مجاز یک **فهرستِ بسته** است (`ALLOWED_WIDTHS`). باز که باشد، یک نفر با `w=1..9999`
  کشِ سرور و CDN را از هزاران آدرسِ بی‌فایده پر می‌کند.
- عمداً فقط webp ساخته می‌شود، نه jpgِ کوچک: مرورگرِ بی‌webp دقیقاً همان چیزی را می‌گیرد که
  امروز می‌گیرد، پس چیزی برایش بدتر نمی‌شود و تعدادِ فایل‌ها هم دو برابر نمی‌شود.
- **دامِ ابزار:** ساختِ بندانگشتی اولش داخلِ شاخه‌ی «اگر webpِ کامل ساخته شد» بود. در اجرای
  دومِ ابزار همه‌ی عکس‌ها «رد‌شده» می‌شوند، پس هیچ بندانگشتی‌ای ساخته نمی‌شد و ابزار بی‌صدا
  کارِ نصفه می‌کرد. حالا `buildFullWebp()` جدا شده تا `return`هایش مرحله‌ی دوم را نپراند.
- **راستی‌آزماییِ تست:** فایل‌های `-320w` را موقتاً برداشتم و دوباره تست گرفتم. فقط تستِ
  «واقعاً کوچک آمد» قرمز شد و بقیه سبز ماندند — یعنی هم تست الکی نیست، هم ادعای «پاک‌کردنِ
  این فایل‌ها چیزی را نمی‌شکند» راست است.
- تستِ نگهبان V26 اندازه‌ی هر سه کادر را از خودِ CSS می‌خواند. اگر روزی کسی `.cart-row-media`
  را بزرگ کند، تست قرمز می‌شود — وگرنه عکس بی‌سروصدا تار می‌شد و هیچ‌کس نمی‌فهمید.

**شمارشِ ارسال مجددِ پیامک** — «زمانِ پایان» در localStorage ذخیره می‌شود نه «ثانیه‌ی مانده»،
پس رفرشِ صفحه شمارش را خراب نمی‌کند و عدد همیشه با سرور همگام است.

**ولعِ خرید** — «فقط N عدد باقی مانده» روی کارت محصول؛ آستانه‌اش از تنظیماتِ پنل می‌آید نه عددِ
ثابت، چون این جمله باید **راست** باشد وگرنه اعتمادِ مشتری می‌رود.

## نسخه‌ی ۸ — جزئیات فنی (۱۴۰۵/۰۵/۰۴)
- **کوپن:** جدول coupons (کد یکتا NOCASE)؛ مصرف از orders شمرده می‌شود (به شرط created_at >= ساخت کوپن — حذف/ساخت مجدد شمارش را صفر می‌کند). کد روی سشن (req.session.coupon) می‌نشیند و در buildCartResponse هر بار دوباره اعتبارسنجی می‌شود؛ ارسال رایگان روی مبلغ بعد از تخفیف. روت‌ها: POST /cart/coupon و /cart/coupon/remove؛ ادمین: GET/POST/PUT/DELETE /admin/coupons.
- **shop-info:** GET /api/shop/info (اعلان/باز-بسته/بنر) و /api/shop/categories و /api/shop/recent-reviews — همه عمومی + ETag. نوار اعلان در common.js (initShopBar؛ رویداد pg:shopinfo). shop_open=0 → POST /orders جواب 503.
- **دسته‌ها:** جدول categories (seed خودکار از محصولات موجود)؛ rename آبشاری روی products (تراکنش)؛ حذف فقط خالی؛ ensureCategory موقع ذخیره‌ی محصول. منو/دراور/کارت‌ها در فرانت از API بازسازی می‌شوند (رویداد pg:cats).
- **باگ حیاتی چک‌اوت:** input های required داخل بخش مخفی، فرم را بی‌صدا بلاک می‌کنند — موقع مخفی‌کردن باید disabled شوند (setNewFieldsEnabled در checkout.js). این درس مهمی است: هر فرم با بخش‌های شرطی باید این را رعایت کند.
- **ویرایش آدرس:** PUT /api/addresses/:id + UI در چک‌اوت (دکمه‌ی ویرایش روی کارت آدرس) و حساب (آیکون مداد؛ فرم پایین با همان دکمه submit دو حالته).
- **سئو:** متاهای og/canonical محصول در server.js داخل HTML تزریق می‌شود (کراولرهای بدون JS)؛ SearchAction در index؛ BreadcrumbList در product.js؛ تستیمونیال‌های قلابی حذف و از /shop/recent-reviews پر می‌شود (id=testiStrip، تا نظری نباشد hidden).
- **بک‌اند:** استاتیک‌ها immutable یک‌ماهه (به‌جز HTML)؛ BACKUP_DIR2 برای بکاپ دیسک دوم؛ PRAGMA optimize در backupNow؛ هشدارهای بوت پروداکشن (NODE_ENV=production)؛ ecosystem.config.js برای pm2.
- **پنل:** ۸ KPI؛ pollFresh هر ۶۰ ثانیه (فقط تب فعال) با توست سفارش/مرجوعی/نظر تازه؛ بخش تخفیف‌ها (view=coupons)؛ مدیریت دسته‌ها داخل «انبار و کالا» (catHost)؛ فاصله‌ی بالای پنل کم شد.
- **هدر:** --header-h از ۱۳۲ به ۱۱۸ (موبایل ۷۸)؛ z-index:30 روی filter-toolbar تا لیست مرتب‌سازی زیر کارت‌ها نرود؛ data-reveal بعد از نمایش transform:none نگه می‌دارد (رفع stacking context).

## نسخه‌ی ۷ — جزئیات فنی
**هزینه ارسال:** تنظیمات `shipping_cost` و `free_shipping_over` (پنل ← تنظیمات) حالا واقعاً کار می‌کنند.
- `getShippingQuote(total)` در db.js تنها منبع حقیقت؛ `/api/cart` فیلدهای shippingFee/freeShippingGap/payable برمی‌گرداند
- سفارش ستون `shipping_fee` دارد و total شامل ارسال است؛ مبلغ درگاه هم همان است
- نوار «X تومان تا ارسال رایگان» در cart.html (کلاس free-ship) فقط وقتی هر دو تنظیم غیرصفرند

**لغو/مرجوعی/رهگیری:** ستون‌های جدید سفارش: `delivered_at`، `return_reason` (tracking_code و cancel_reason از قبل بود).
- وضعیت‌های جدید: `return_requested` و `returned`؛ گراف: paid→canceled (مشتری فقط paid)، delivered→return_requested (مشتری، تا ۷ روز بعد از delivered_at)، return_requested→returned (تأیید ادمین + برگشت موجودی، فقط از مسیر adminAcceptReturnTx) یا →delivered (رد)
- روت‌های مشتری: POST `/api/orders/:id/cancel` و `/api/orders/:id/return` (دلیل ≥۵ حرف)
- PAID_SET درآمد شامل return_requested هست؛ returned و canceled خارج‌اند
- حساب کاربری: دکمه‌ها + فرم دلیل داخل کارت (کلاس‌های order-actions/oa-*) + نمایش کد رهگیری (order-track)

**نظرات:** جدول `reviews` (یکتا روی product_id+user_id؛ ویرایش → برگشت به pending).
- تشخیص «خریدار» با LIKE روی items سفارش‌های پرداختی (hasUserBought)
- ETag کاتالوگ با شمارنده‌ی `reviews_rev` در settings باطل می‌شود (هر ثبت/تأیید/رد یکی زیاد می‌کند)
- روت‌ها: GET/POST `/api/products/:id/reviews`؛ ادمین: GET `/api/admin/reviews?status=` و POST `/api/admin/reviews/:id/status`
- پنل بخش جدید «نظرات» (view=reviews) با چیپ وضعیت و شمارنده‌ی nav (stats.pending_reviews)
- صفحه‌ی محصول: بخش pdReviews (rv-*) + ستاره کنار عنوان + aggregateRating در Schema فقط با count>0

**گالری/مشخصات:** ستون‌های products: `images` و `specs` (هر دو JSON).
- اعتبارسنجی ادمین: حداکثر ۸ عکس همه از /picture؛ حداکثر ۱۲ ردیف مشخصه (k≤40, v≤120)
- مودال پنل: pmGallery (آپلود چندتایی از همان endpoint) + pmSpecs (ردیف عنوان/مقدار)
- صفحه‌ی محصول: بندانگشتی‌ها (pd-thumbs) + جدول pd-specs

**نکته‌ی تست:** محدودکننده‌ی سراسریِ نوشتن (۶۰ در دقیقه در server.js) حالا با env قابل بازکردن است:
تست اسموک `WRITE_RATE_LIMIT=10000` و `API_RATE_LIMIT=10000` را خودش ست می‌کند. تست ردپا نمی‌گذارد
(تنظیمات و محصول را به حالت قبل برمی‌گرداند و نظر تستی را reject می‌کند).

## نسخه‌ی ۶ — جزئیات (۱۴۰۵/۰۵/۰۳ و ۰۴)
**بک‌اند:**
- ETag/304 روی /api/products (لیست ۳۰ ثانیه، فیلترشده ۱۲۰ ثانیه، تکی بر اساس updated_at+stock)
- صفحه‌بندی/فیلتر سمت دیتابیس: page/limit/q/category/sort/minPrice/maxPrice/inStock + meta
- WAL با فال‌بک خودکار به DELETE (پوشه‌های شبکه‌ای/مانت‌شده WAL را درست ساپورت نمی‌کنند)
- ۹ ایندکس (orders، products، sessions، wishlist، addresses، admin_log)
- /api/health (سبک) و /api/health?full=1 (با تست دیتابیس)
- تست‌ها از ۴۹ به **۶۶** رسید (ETag، صفحه‌بندی، تزریق، سلامت)
- ابزار: backend/tools/css-audit.js (کلاس‌های CSS بی‌استفاده را لیست می‌کند)

**فرانت — صفحه‌ی ورود (login.html) از نو:**
- بدون هدر/منو/فوتر/نوار پایین (body class="auth-page" + data-no-bottom-nav)
- پس‌زمینه: ۳ هاله‌ی رنگی متحرک (ab-blob) + ۶ آیکون شناور پلاسکویی (ab-float)
- کارت شیشه‌ای با انیمیشن ورود + نوار رنگی متحرک بالای کارت + چیپ‌های اعتماد
- دکمه‌ی ثابت «بازگشت به فروشگاه» بالا-راست
- **بعد از ورود → index.html** (نه پروفایل!) مگر ?next= داشته باشد (checkout و admin می‌فرستند)

**فرانت — تجربه‌ی خرید:**
- مرتب‌سازی: منوی سفارشی هم‌رنگ تم جای لیست سفید ویندوزیِ select (main.js → buildCustomSort
  داخل initFilterToolbar؛ select مخفی منبع حقیقت می‌ماند و change روی آن dispatch می‌شود)
- توست addToCart لینک «مشاهده سبد» دارد (common.js → toast با opts.action)
- دکمه‌ی خرید صفحه‌ی محصول بازخورد «اضافه شد» گرفت (product.js)
- نشانه‌های اعتماد زیر دکمه‌ی تکمیل خرید در cart.html (کلاس summary-trust)
- شمارنده‌ی نتیجه فقط با فیلتر فعال («X محصول پیدا شد»)، بدون فیلتر خالی
- دکمه‌ی «ورود» هیروی صفحه‌ی اصلی حالا data-auth-link است — کاربر واردشده اسمش را می‌بیند
- تب‌های حساب در موبایل: قرص یک‌خطی اسکرولی (بلوک 900px، account-nav)
- فرم آدرس حساب: کلاس form-grid-2 جای style درون‌خطی (موبایل یک‌ستونه)
- فاصله‌ی بالای صفحه: --header-h از 142 به 132، پدینگ هیرو clamp شد

## پنل مدیریت (admin.html)
- فعال‌سازی: در `backend/.env` بنویس `ADMIN_PHONE=09113567409` و سرور رو ری‌استارت کن.
- ۸ بخش: داشبورد، سفارش‌ها، محصولات، مشتری‌ها، نظرات، تخفیف‌ها، گزارش‌ها، لاگ عملیات، تنظیمات
- بازسازی ۱۴۰۵/۰۵/۰۳؛ برگشت اضطراری: backup/_backup-admin-20260725-144158
- امنیت: فقط شماره‌ی ADMIN_PHONE (پرچم is_admin + چک سمت سرور همه‌ی مسیرها)
- نسخه‌ی ۹: قفلِ حساب بعد از رمزِ غلطِ پیاپی، انقضای بی‌کاری ۳۰ دقیقه، خروجی CSV

## امنیت
- هدرها: CSP، Permissions-Policy، HSTS روی HTTPS، X-Frame و بقیه
- rate limit جدا روی چک کد ورود؛ سقف ۵۰ نوع کالا در سبد
- آپلود عکس: چک امضای واقعی فایل، حداکثر ۲MB، **حداکثر ۴۰۰۰ و حداقل ۸۰ پیکسل**، نام تصادفی؛
  مسیر فقط /picture/ بدون ..
- **پاک‌سازی فرادادهٔ عکس** (نسخه‌ی ۱۰، `lib/image-clean.js`): GPS و مدلِ گوشی و مسیرِ فایل
  قبل از نوشتن روی دیسک حذف می‌شوند. بدونِ کتابخانه‌ی بیرونی و بدونِ دست‌زدن به پیکسل‌ها،
  پس نه افتِ کیفیت دارد نه وابستگیِ باینری. عکسی که تگِ چرخش دارد عمداً دست‌نخورده می‌ماند
  (حذفِ تگ، عکسِ عمودی را خوابیده نشان می‌دهد). چهار تستِ امنیت فایلِ **روی دیسک** را می‌خوانند.
- **شمارشِ عمومی نباید پیش‌نویس را لو بدهد** (نسخه‌ی ۱۰): مسیرِ عمومیِ `/api/shop/categories`
  از `getPublicCategories()` می‌آید که فقط منتشرشده‌ها را می‌شمرد و دسته‌ی خالی را حذف می‌کند؛
  `getCategoriesFull()` که `countAll` دارد فقط برای پنل است. سه تست این مرز را قفل کرده‌اند.
- قیمت سفارش از دیتابیس، رزرو اتمی موجودی، ضد session fixation و timing attack
- قفلِ ورودِ مدیر به‌ازای هر حساب + دفتر رویداد با شماره‌ی ماسک‌شده (نسخه‌ی ۹)
- پیام‌های خطا عمداً یک‌شکل‌اند: «کاربر نیست» و «رمز غلط» و «سفارش پیدا نشد» فرقی نمی‌کنند،
  وگرنه خودِ تفاوتِ پیام یک ابزارِ شناسایی می‌شد

## تست خودکار — ✅ ۷۱۹ پاس (۱۴۰۵/۰۵/۱۰)
    cd /d E:\claude\polasco-goli\backend
    node test-all.js
سه مجموعه را پشت‌سرهم می‌گیرد: دود **۶۴۱**، امنیت **۴۱**، تخفیف **۳۷** — جمعاً **۷۱۹**.
(عددِ درست را همیشه از خطِ `SUCCESS: all N tests passed` خروجی بردار، نه از این یادداشت —
این عدد دو بار در گذشته کهنه شده بود.)
(اگر فقط یکی را می‌خواهی: `node test-smoke.js`)
خروجی انگلیسیه چون CMD ویندوز فارسی رو مربع نشون می‌ده. هر خط [FAIL] رو برای کلود بفرست.

**تست هیچ‌وقت به داده‌ی واقعی دست نمی‌زند.** سه چیز کپی می‌شود، نه اصل:
`PG_DATA_DIR` (دیتابیس)، `LOG_DIR` (لاگ)، و `PG_PICTURE_DIR` (پوشه‌ی عکس).
تاریخِ این دیوار سه اتفاقِ واقعی است: یک بار تست با انتخابِ «اولین کالای موجود» ۵۴ عدد از
موجودیِ واقعیِ مغازه را خورد؛ `cleanupOldLogs()` می‌توانست لاگ‌های واقعی را پاک کند؛ و
تستِ امنیت دو فایلِ آشغالِ `p-….png` در پوشه‌ی عکسِ واقعی جا گذاشت (چون آخرِ کار
پاک‌کردن شکست خورد). شش تستِ «V17 دیوار» اگر کسی هر یک از این سه محافظ را بردارد، بلند
داد می‌زنند — هر شش‌تا با خراب‌کردنِ عمدیِ کد امتحان شده‌اند و واقعاً قرمز می‌شوند.

## ابزارهای کمکی (از پوشه‌ی backend اجرا شوند)
    node tools/css-audit.js          # کلاس‌های CSS بی‌استفاده
    node tools/contrast-audit.js     # کنتراست رنگ در سطح WCAG 2.2 AA
    node tools/optimize-images.js    # نسخه‌ی WebP + نسخه‌های ۳۲۰px و ۵۶۰px کنار عکس‌ها
    node tidy-test-data.js           # پاک‌کردنِ داده‌ی تستی (اختیاری) — این یکی در tools/ نیست
    node tools/restore-backup.js     # برگرداندن دیتابیس از بکاپ (بی‌آرگومان: فقط فهرست)
    node tools/seed-catalog.js       # ساختِ کالاهای پیش‌نویس (--rollback برای برگرداندن)

## گیت (از ۱۴۰۵/۰۵/۰۹)
    cd /d E:\claude\polasco-goli
    git status                 # چه چیزهایی عوض شده
    git diff                   # دقیقاً چه خطی عوض شده
    git log --oneline          # تاریخچه
    git checkout -- <فایل>     # برگرداندنِ یک فایل به آخرین حالتِ سالم
اگر کلود چیزی را خراب کرد، دیگر لازم نیست کلِ پوشه را از بکاپ برگردانی — همین
`git checkout` کافی است. **ولی حواست باشد:** دیتابیس عمداً بیرونِ گیت است، پس گیت
سفارش‌ها و مشتری‌ها را برنمی‌گرداند. برای آن هنوز `tools/restore-backup.js` است.

چهار چیز هیچ‌وقت نباید وارد گیت شوند و در `.gitignore` هستند: فایل `.env` (کلیدها)،
`backend/data/*.db` به‌همراه `-wal`/`-shm`/`backups/` (شماره و آدرس و کدپستیِ واقعیِ
مشتری‌ها)، `backend/logs/`، و `node_modules`. اگر روزی این پروژه را روی GitHub گذاشتی،
قبلش با `git status` مطمئن شو هیچ‌کدام دیده نمی‌شوند. **دلیلِ سخت‌گیری:** فایلی که یک
بار کامیت شد در تاریخچه می‌ماند؛ پاک‌کردنش بعداً تقریباً غیرممکن است.

## ⚠️ نکته‌های فنی مهم (تجربه‌های دردناک)
- **کش‌شکنی:** بعد از هر تغییر css/js، عدد `?v=` را در **همه‌ی** HTMLها یکی زیاد کن (الان **v=41**).
  ۱۳ فایل HTML این عدد را دارند؛ `offline.html` عمداً ندارد (سرویس‌ورکر خودش نسخه دارد).
  تغییرِ فقط HTML بمپ نمی‌خواهد — HTML خودش کش نمی‌شود.
- **اگر `assets/icons.svg` عوض شد، نسخه‌ی کشِ سرویس‌ورکر را هم بمپ کن** (`pg-static-v…` در
  `sw.js`، الان **v3**). این فایل جزو پیش‌کش است، پس بدونِ بمپ کاربرِ قدیمی نسخه‌ی بدونِ
  آیکونِ تازه را می‌بیند و به‌جای فلش یک مربعِ خالی می‌بیند. تستِ V10 حداقلِ نسخه را
  می‌سنجد (`SW_MIN_VERSION` در `test-smoke.js` را هم‌زمان بالا ببر).
- **دیتابیس روی پوشه‌ی مانت‌شده:** checkpoint از نوع TRUNCATE فایل‌های -shm/-wal را خراب می‌کند؛
  در closeDb فقط PASSIVE بزن. اگر فایل‌های جانبی قفل شدند یعنی سرور ویندوزی روشن است — اول اون بسته شود.
- **انیمیشن از حالت نامرئی شروع نشود** مگر خروجی واقعی دیده شود (درس نسخه‌ی ۵: کارت‌های نامرئی).
- هر انیمیشن جدید باید در بلوک prefers-reduced-motion خاموش شود.
- تست‌ها را قبل از تحویل با سرور روشن بگیر؛ سرور پس‌زمینه‌ی قدیمی → اندازه‌گیری غلط.
- test-data قدیمی (کاربرهای ۰۹۱۲…) را پاک نکن — داشبورد ادمین را پر نشان می‌دهند.
  تمیزکاری فقط با backend/tidy-test-data.js (اگر خواستی).
- **مسیرِ پوشه‌ی عکس را هیچ‌جا دستی ننویس.** فقط از `lib/paths.js` بگیر:
  `server.js` عکس را از آنجا سرو می‌کند و `routes/admin.js` آپلود را همان‌جا می‌گذارد.
  اگر یکی‌شان مسیرِ ثابتِ خودش را داشته باشد، این دو از هم جدا می‌شوند و آپلودِ ادمین
  در جایی می‌نشیند که سرور نگاهش نمی‌کند — عکس آپلود می‌شود ولی نشان داده نمی‌شود.
  یک نگهبانِ V17 همین یکی‌بودن را می‌سنجد.
- **تنظیماتِ فروشگاه بعد از تست را چک کن.** `getSettings()` کش ندارد و مستقیم از دیتابیس
  می‌خواند، پس هر عددِ غلطی که آنجا بنشیند بلافاصله به مشتری نشان داده می‌شود. باگِ
  «۸۹ میلیون تومان تا ارسال رایگان» دقیقاً همین بود. اگر عددی مشکوک دیدی، در جدول
  `admin_log` بگرد — تاریخ و منبعِ تغییر آنجاست.
- **`datetime('now')` در SQLite فقط تا ثانیه دقت دارد.** هیچ‌وقت آن را تنها معیارِ
  «چیزی عوض شد؟» نگذار. باگِ نسخه‌ی ۱۱ همین بود: دو ویرایش در یک ثانیه، امضای یکسان،
  و ویرایشِ دوم تا تغییرِ بعدیِ کاتالوگ نامرئی. برای تشخیصِ تغییر، **شمارنده** بگذار
  نه زمان (`catalog_rev` در `lib/db.js`).
- **برای باطل‌کردنِ کش، تریگرِ دیتابیس امن‌تر از کدِ جاوااسکریپت است.** روی `products`
  از ۸ جای مختلف نوشته می‌شود؛ اگر باطل‌سازی دستی بود، یک جای فراموش‌شده کافی بود که
  باگ بی‌صدا برگردد. از تریگر هیچ مسیری فرار نمی‌کند.
- **تستی که قرمز شد را دور نریز، اول بفهم چرا.** دو بار در همین پروژه تستِ «غلطِ من»
  باگِ واقعیِ کد را لو داد: یک بار `fetch` بدنه را خودش از حالت فشرده درآورد و من فکر
  کردم فشرده‌سازی خراب است، و یک بار اتصالِ دومی به دیتابیس امضا را عوض نکرد که از
  دلش باگِ ثانیه بیرون آمد.

## چیزهایی که برای فروش واقعی مونده
1. کلید سرویس پیامک (Kavenegar یا ippanel یا Farapayamak) — توی backend/.env
2. کد پذیرندگی زرین‌پال (Merchant ID) — توی backend/.env
3. ADMIN_PHONE توی .env (برای پنل مدیریت)
4. **عکس‌ها** — این بزرگ‌ترین کارِ باقی‌مانده است: ۸ کالای منتشرشده هنوز بی‌عکس‌اند،
   و ۸۸ کالای پیش‌نویس تا عکس نگیرند روی سایت نمی‌آیند. در پنل → انبار و کالا،
   فیلترِ «بدون عکس» را بزن.
5. موقع رفتن روی دامنه‌ی SSL‌دار: `COOKIE_SECURE=true` در .env
   (الان عمداً false است — روی HTTP اگر true باشد اصلاً نمی‌شود وارد شد)
موردهای ۱ تا ۴ رو فقط صاحب کسب‌وکار می‌تونه انجام بده.
موردهای ۱ و ۲ و ۵ رو خودِ سرور موقع بالاآمدن با `NODE_ENV=production` یادآوری می‌کند
(چک‌لیستِ `server.js`، امتحان شده — هر سه هشدار را انگلیسی چاپ می‌کند).

## ایده‌های بعدی
**این فهرست خالی شد** — هر سه مورد انجام شد:
- ✅ PWA (`sw.js` + `manifest.webmanifest` + `offline.html`) — نسخه‌ی ۹
- ✅ بندانگشتیِ کم‌حجم برای جست‌وجو/سبد/پنل — `?w=320`، بالاتر توضیح داده شد
- ✅ پیامکِ خودکارِ تغییر وضعیت سفارش — از قبل ساخته و وصل بود (`notifyCustomerOrderStatus`
  در `lib/sms.js`، صدا زده شده در سه جای `routes/admin.js` و یک جای `routes/orders.js`؛
  متن برای paid/shipped/delivered/canceled/returned جداست و کدِ رهگیری هم داخلش می‌آید).
  این مورد اشتباهاً توی فهرستِ «مانده» بود.

آن سه کاندیدِ بعدی هم انجام شدند — هر سه پایین توضیح داده شده‌اند:
- ✅ عکسِ کارتِ محصول روی موبایل (`?w=560`)
- ✅ ابزارِ بازگردانیِ بکاپ (`tools/restore-backup.js`)
- ✅ بخشِ «خطاها» در پنل

✅ گزارشِ فروشِ ماهانه هم انجام شد (نسخه‌ی ۴۰، پایین توضیح داده شده) — یعنی این فهرست
دوباره خالی است.

✅ `LOG_DIR` هم درست شد (نسخه‌ی ۴۰). یادداشتِ قبلی می‌گفت «خطری ندارد، لاگ فقط اضافه
می‌شود» — **این حرف اشتباه بود.** لاگ فقط اضافه نمی‌شد: `cleanupOldLogs()` هر چیزی را که
داخل `LOG_DIR` باشد و بیشتر از ۱۴ روز عمر داشته باشد **پاک می‌کند**، و چون `LOG_DIR`
همیشه `backend/logs` بود، هر اجرای تست می‌توانست لاگ‌های واقعیِ مغازه را حذف کند. حالا
`lib/logger.js` دقیقاً مثل `lib/db.js` به `PG_DATA_DIR` گوش می‌دهد و لاگِ تست داخل
همان پوشه‌ی موقتی می‌نشیند. یک نگهبان هم زیرِ «V17 دیوار» اضافه شد که اگر کسی این خط را
بردارد، تست همان لحظه قرمز شود (امتحانش کردم: قرمز می‌شود و مسیرِ اشتباه را چاپ می‌کند).

از این به بعد تست‌های V29 می‌توانند به‌جای *شکلِ* پاسخ، خودِ عدد را هم بسنجند — چون دیگر
پوشه‌ی لاگ در هر اجرا تمیز و قابلِ پیش‌بینی است. (هنوز انجام نشده، فقط ممکن شده.)

کاندیدهای بعدی: فعلاً فهرست خالی است.

---

### کارتِ محصول روی موبایل (`?w=560`) — نسخه‌ی ۳۹
**نتیجه‌ی اندازه‌گیری برعکسِ حدسِ اولیه بود.** فرض این بود که موبایل عکسِ کوچک‌تر می‌خواهد؛
اندازه‌گیری نشان داد کارتِ یک‌ستونیِ موبایل **۳۸۲px** است و کارتِ چهارستونیِ دسکتاپ **۲۶۸px** —
یعنی موبایل *بزرگ‌ترین* کارت را دارد. اگر حدس را پیاده کرده بودیم، کیفیت روی موبایل خراب می‌شد.
تنها صرفه‌جوییِ صادقانه صفحه‌های **DPR۱** بود: یک نسخه‌ی ۵۶۰px که بدترین حالت (۵۱۶px) را با
۸٪ حاشیه پوشش می‌دهد، پس به عرضِ پنجره وابسته نیست و با تغییرِ اندازه‌ی پنجره چیزی خراب نمی‌شود.
صرفه‌جویی: **۲۸۰KB → ۱۳۳KB** (۵۳٪) روی صفحه‌های DPR۱. `PG.cardImg()` در `common.js`.

### `tools/restore-backup.js` — نسخه‌ی ۳۹
خطرناک‌ترین ابزارِ پروژه، پس پنج لایه محافظ دارد: بی‌آرگومان فقط فهرست می‌دهد؛ بکاپ قبل از
اعتماد با `integrity_check` بررسی می‌شود؛ قبل از بازنویسی از وضعیتِ فعلی عکس گرفته می‌شود
(`pre-restore-*.db`) و اگر عکس نشد کلاً کار را انجام نمی‌دهد؛ تفاوت را نشان می‌دهد و `yes`
می‌خواهد؛ و فایل‌های کهنه‌ی `-wal`/`-shm` را پاک می‌کند (اگر بمانند، SQLite تغییراتِ نیمه‌کاره‌ی
دیتابیسِ *قبلی* را روی فایلِ تازه پخش می‌کند — همان چیزی که بازگردانیِ دستی را خطرناک می‌کرد).
آخرِ کار دستورِ دقیقِ برگشت را چاپ می‌کند.

ضمنِ نوشتنش یک باگِ **از قبل موجود** در چرخشِ بکاپ‌های `lib/db.js` پیدا شد: شرطِ
`f.endsWith('.db')` عکس‌های دستی را هم در سهمیه‌ی ۱۴ روز می‌شمرد ولی خودشان الفبایی بعد از
`polasco-` می‌افتادند و هرگز پاک نمی‌شدند — یعنی هر عکسِ دستی یک روز از تاریخچه را می‌خورد.
با `/^polasco-.*\.db$/` در **هر دو** جای چرخش درست شد.

### بخشِ «خطاها» در پنل — نسخه‌ی ۳۹
`lib/error-digest.js` + `GET /api/admin/errors` + بخشِ «خطاها». لاگ‌ها از قبل در
`logs/error-*.log` نوشته می‌شدند ولی خواندنشان SSH لازم داشت، پس عملاً خوانده نمی‌شدند.
خامش را نشان نمی‌دهیم: خطاهای مشابه گروه می‌شوند («این خطا ۱۲ بار، آخرین بار ۲ ساعت پیش»)،
شماره‌ی موبایل با همان قاعده‌ی `routes/auth.js` ماسک می‌شود، فریم‌های `node_modules` و داخلیِ
Node انداخته می‌شوند و مسیرِ پروژه از stack بریده می‌شود، و از هر فایل فقط ۵۱۲KBِ آخر خوانده
می‌شود تا پنل حافظه‌ی سرور را نخورد.

سه چیز فقط چون روی **لاگِ واقعی** امتحان شد پیدا شد — نمونه‌ی دست‌ساز هیچ‌کدام را نشان نداد:
- **۵۰۳ نباید خطا حساب شود.** اول «هر چیزِ ≥۵۰۰» شمرده می‌شد و عددِ ۵۶ درآمد با صفر خطای
  ثبت‌شده. هر ۵۶ مورد `POST /api/orders` بود با پاسخِ «فروشگاه موقتاً تعطیل است» — تصمیمِ خودِ
  صاحبِ مغازه، نه خرابی. کارتِ «مشتری خطا دید» بی‌دلیل قرمزِ ۵۶ می‌شد.
- فیلترِ فریم‌های داخلی فقط `node:internal` را می‌گرفت، پس `(node:_http_outgoing:…)` و
  `at node:internal/main/…` (بی‌پرانتز) رد می‌شدند.
- لاگ ماه‌ها می‌ماند و ممکن است از زمانی باشد که پروژه در مسیرِ دیگری نصب بوده؛ آن فریم‌ها با
  `rootDir`ِ امروز کوتاه نمی‌شدند. حالا هر چه قبل از `/backend/` یا `/frontend/` باشد بریده می‌شود.

یک باگ هم موقعِ اجرا لو رفت که هیچ تستی نمی‌دیدش: شرطِ داخلِ مسیر `req.user` نوشته شده بود،
در حالی که `requireAdmin` مقدار را در **`req.adminUser`** می‌گذارد — یعنی مسیر برای *ادمین هم*
۴۰۳ می‌داد، و فرانت این را بی‌صدا به «پوشه‌ی لاگ خوانده نشد» ترجمه می‌کرد.

### گزارشِ فروشِ ماه‌به‌ماه (شمسی) — نسخه‌ی ۴۰
`lib/jalali.js` + `getMonthlySales()` در `lib/db.js` + `GET /api/admin/reports/monthly` +
`GET /api/admin/export/monthly.csv` + کارتِ «ماه‌به‌ماه» در بخشِ گزارشِ پنل.

عمداً از نمودارِ «۳۰ روز اخیر» جداست و انتخابگرِ بازه‌ی خودش را دارد: «۳۰ روزِ اخیر» و «مرداد»
دو سؤالِ متفاوت‌اند و یکی جایگزینِ دیگری نمی‌شود.

چند تصمیمِ ریز که مهم شدند:
- تقویم با `Intl.DateTimeFormat('en-u-ca-persian')` ساخته می‌شود، نه `fa-IR`، چون ارقام باید
  لاتین بمانند تا `Number()` رویشان کار کند. سالِ خروجی چک می‌شود که حدودِ ۱۴۰۰ باشد نه ۲۰۲۶؛
  اگر نودِ سرور با ICUِ کوچک ساخته شده باشد، `available()` مقدارِ false می‌دهد و گزارش با
  ماهِ میلادی کار می‌کند و بالای کارت به کاربر هشدار می‌دهد — نه اینکه پنل خطا بدهد.
- هیچ منطقه‌ی زمانی hardcode نشده تا مرزِ ماه دقیقاً با `date(created_at,'localtime')`ِ
  SQLite یکی باشد. `created_at` به UTC ذخیره می‌شود، پس `localtime` باید **هر دو طرفِ**
  مقایسه بیاید وگرنه سفارش‌های شبانه یک ماه جابه‌جا می‌افتند.
- ماه‌های بی‌فروش با صفر می‌آیند، حذف نمی‌شوند؛ وگرنه جدول شکاف دارد و «رشد» بی‌معنی می‌شود.
- **رشد وقتی ماهِ قبل صفر بوده `null` است، نه ۱۰۰٪ و نه بی‌نهایت.** «از صفر به ۵ میلیون»
  درصدِ معنادار ندارد و عددِ ساختگی کلِ گزارش را بی‌اعتبار می‌کند.

**درسِ تست (مهم):** اولین نسخه‌ی تستِ همین قاعده‌ی `null` کاملاً **کور** بود. با شکستنِ عمدیِ
کد (برداشتنِ شرطِ `prev.sales > 0`) تست‌ها سبز ماندند، چون `JSON.stringify` هم `NaN` و هم
`Infinity` را به `null` تبدیل می‌کند و از روی پاسخِ API اصلاً قابلِ تشخیص نیستند. حالا این
قاعده جایی سنجیده می‌شود که واقعاً دیده می‌شود: `getMonthlySales()` مستقیم در همان پروسه صدا
زده می‌شود (مقدارِ خامِ جاوااسکریپت)، به‌علاوه‌ی خودِ فایلِ CSV — که برخلافِ JSON مقدارِ خام را
می‌نویسد و با کدِ شکسته واقعاً `NaN` و `Infinity` داخلِ فایلِ اکسل می‌ریخت.

### رمزِ ناهمگام و باطل‌کردنِ نشست‌ها — نسخه‌ی ۴۰
دو ایرادِ واقعی که با سه سؤالِ صاحبِ مغازه پیدا شدند (هزار ورود از یک نفر / هزار نفر در یک
دقیقه / دزدیده‌شدنِ کوکی):

**۱) `crypto.scryptSync` روی event loop.** نود تک‌نخی است و scrypt عمداً کُند است — روی همین
سرور اندازه گرفته شد: **۲۶.۱ms**. نسخه‌ی `Sync` این ۲۶ms را روی خودِ حلقه می‌سوزاند، یعنی در
تمامِ آن مدت *کلِ سایت* متوقف است، نه فقط لاگین. سقفِ عملی می‌شد ~۳۸ ورود در ثانیه.
بدتر از کندی، این یک **اهرمِ حمله** بود: `verifyPassword` روی مسیرِ *قبل از* احراز هویت است،
پس رمزِ غلط هم همان ۲۶ms را از سرور می‌گرفت — درخواستِ ارزان، هزینه‌ی گرانِ سرور. سقف‌های نرخ
تعداد را کم می‌کنند ولی هزینه‌ی هرکدام را نه.
با `promisify(crypto.scrypt)` کار به استخرِ نخِ libuv رفت. همان الگوریتم و همان پارامترها،
پس رمزهای قبلی همچنان کار می‌کنند (یک تست هش را با `scryptSync` بازتولید می‌کند و
بیت‌به‌بیت مقایسه می‌شود). اندازه‌گیریِ واقعی روی ۲ هسته با ۱۶ هشِ هم‌زمان: درخواستِ یک
مشتریِ دیگر **۳۹۹ms → ۳ms** (حدود ۱۳۳ برابر)، و کلِ کار ۳۹۹ms → ۲۲۶ms.

> پروبِ اولِ من برای این اندازه‌گیری **غلط** بود: با `setInterval` نوشته بودم و «۰ms توقف»
> گزارش داد، چون تا وقتی حلقه بسته است خودِ تایمر هم فرصتِ اجرا پیدا نمی‌کند. با یک
> `setTimeout(…,0)` که نقشِ «درخواستِ مشتریِ بعدی» را دارد بازنویسی شد.

**۲) کوکیِ نشست غیرقابلِ ابطال بود.** کوکی `httpOnly` است و CSP هم `script-src 'self'` دارد،
پس جاوااسکریپتِ تزریقی نمی‌تواند بخواندش. ولی *اگر* به هر راهِ دیگری لو می‌رفت (گوشیِ گم‌شده،
کامپیوترِ عمومی، بدافزار) **۳۰ روز** معتبر بود و هیچ راهی برای کشتنش نبود — حتی عوض‌کردنِ رمز
هم نشستِ بازِ مهاجم را دست‌نخورده می‌گذاشت.
حالا `destroyOtherSessions()` در `lib/session-store.js` با `json_extract(data,'$.userId')`
نشست‌های همان کاربر را پاک می‌کند (جدولِ sessions فقط sid/data/expires_at دارد، پس بدونِ
تغییرِ ساختارِ جدول). پارامترِ `keepSid` نشستِ خودِ همان مرورگر را نگه می‌دارد وگرنه کاربر با
عوض‌کردنِ رمزِ خودش از حسابِ خودش هم پرت می‌شود بیرون. وصل شده به `POST /auth/password/set`
(خودکار) و `POST /auth/logout-others` (دکمه‌ی «خروج از همه‌ی دستگاه‌های دیگر» در صفحه‌ی حساب)،
به‌علاوه‌ی `GET /auth/sessions` برای نشان‌دادنِ «۳ دستگاه وارد است».

درباره‌ی نگرانیِ «آدرس را عوض کند و بفرستد درِ خانه‌ی خودش»: این یکی از قبل امن بود.
`createOrderTx` آدرس را به‌صورتِ **عکسِ لحظه‌ای** داخلِ خودِ ردیفِ سفارش ذخیره می‌کند، پس
دست‌کاریِ دفترچه‌ی آدرس روی سفارشِ ثبت‌شده اثری ندارد و هیچ مسیرِ سمتِ مشتری هم آدرسِ یک
سفارشِ موجود را عوض نمی‌کند.

**دو تستِ نگهبان که جایشان را ثابت کردند** (با شکستنِ عمدیِ کد سنجیده شد):
- «هر فراخوانیِ `verifyPassword` با `await` است» — اگر کسی `await` را بردارد، تابع یک Promise
  برمی‌گرداند و Promise **همیشه** truthy است، یعنی `!verifyPassword(...)` همیشه false می‌شود و
  سرور *هر رمزی* را قبول می‌کند. با حذفِ آزمایشیِ `await`، رمزِ غلط واقعاً پذیرفته شد.
- «نشستِ کاربرانِ دیگر دست‌نخورده ماند» — با خراب‌کردنِ شرطِ `json_extract`، دستور نشستِ
  *همه‌ی* کاربرانِ سایت را پاک می‌کرد و این تنها تستی بود که گرفتش.

## نکته درباره‌ی کار با کلود
- به فارسی خودمونی صحبت می‌کنیم و جواب‌ها فارسی باشه
- کلود کارها رو خودش کامل انجام بده و دستور آماده‌ی کپی بده
- دیجی‌کالا الگوی ظاهر و تجربه‌ی کاربریه
- تغییرات ظاهری: کم‌کم و باریک، نه بازطراحی یک‌جا (بازطراحی کامل v8 رد شد و برگشت خورد)
- هر پیامی که قراره توی CMD چاپ بشه انگلیسی باشه، ولی خود سایت فارسی می‌مونه
- قبل از هر تغییر بزرگ، بکاپ گرفته می‌شه (پوشه‌ی backup/ داخل پروژه)
  آخرین بکاپ‌ها: **backup/pre-v9-20260729-045749 (کلِ نسخه‌ی ۹ — برگشت اضطراری)**،
  backup/front-20260726-113034 (فرانت + روت‌های بک‌اند)، backup/full-20260725-235825 (کامل)
- گزارشِ ادعایی از بازبینیِ کد را کورکورانه اعمال نکن؛ چند بار پیش آمده که تیترِ گزارش ساختگی
  بوده. اول خودِ فایل را بخوان، بعد تصمیم بگیر.
