# گزارش: رفتن به ۱۰۰ محصول — چه چیزی لازم است و چه چیزی لازم نیست

تاریخ: ۱۴۰۵/۰۵/۰۹ (2026-07-31)
وضعیت: **گزارش، قبل از اجرا.** هیچ کدی هنوز عوض نشده.

---

## ۰. یک هشدار قبل از هر چیز

برای این گزارش می‌خواستم ساختار فروشگاه‌های بزرگ فارسی را از وب بخوانم. نشد:

- `WebSearch` دو بار خطای API داد.
- زیرعاملی که فرستاده بودم گزارش داد نتایج جست‌وجو بی‌ربط بودند و **داخلشان متنی بود که خودش را جای دستور جا می‌زد**. نادیده گرفته شد، ولی باید بدانی چنین چیزی دیده شد.

پس هر جا در این گزارش از «فروشگاه‌های بزرگ» حرف می‌زنم، **دانش داخلی من است، نه منبع تازه**. در مقابل، هر عددی که می‌بینی از اندازه‌گیری روی کد خودت درآمده و قابل بازتولید است.

---

## ۱. چرا کند است؟ (جواب سؤال اول)

روی داده‌ی امروز — ۱۲ محصول، ۳۳۳ سفارش — **هیچ چیز کند نیست**. همه زیر یک میلی‌ثانیه. برای همین یک نسخه‌ی بزرگ‌شده ساختم (۵۰۱۲ محصول، ۳۰۳۳۳ سفارش) و دوباره اندازه گرفتم:

| عملیات | زمان در مقیاس بزرگ |
|---|---|
| `queryProducts` صفحه‌ی ۱ | ۱٫۶۳ ms |
| `queryProducts` صفحه‌ی ۴۰۰ | ۶٫۰۵ ms ⚠ |
| جست‌وجوی فازی (غلط املایی) | ۶٫۴۳ ms ⚠ |
| `getTopProducts` | ۵٫۲۶ ms ⚠ |
| **`getAdminStats`** | **۲۶٫۴۰ ms** ⚠ |
| `queryOrders` صفحه‌ی ۱ | ۱۰٫۳۳ ms ⚠ |
| **`getProductsWithSales`** | **۲۷٫۵۵ ms** ⚠ |
| `getOrderStatusCounts` | ۰٫۷۵ ms |

### ریشه‌ی مشکل، با مدرک

وقتی ستون را داخل تابع می‌پیچی، ایندکس از کار می‌افتد. با `EXPLAIN QUERY PLAN` ثابتش کردم:

```sql
-- الان اینطور است (db.js خط ۹۹۰ تا ۱۰۱۲)
WHERE date(created_at,'localtime') = date('now','localtime')
-- نقشه: SEARCH orders USING COVERING INDEX idx_orders_status (status=?)
-- یعنی همه‌ی سطرهای آن وضعیت را می‌خواند → ۳٫۸۳ ms

-- باید اینطور باشد
WHERE created_at >= '2026-07-31' AND created_at < '2026-08-01'
-- نقشه: SEARCH ... (status=? AND created_at>? AND created_at<?)
-- → ۰٫۰۱ ms
```

**۳۸۰ برابر اختلاف.** و `stmtStats` شش تا از این زیرکوئری‌ها دارد.

چرا این مهم‌تر از یک عدد ساده است: `node:sqlite` **همگام (synchronous)** است. این ۲۶ میلی‌ثانیه فقط داشبورد ادمین را کند نمی‌کند — کل سرور را برای ۲۶ میلی‌ثانیه قفل می‌کند. مشتری‌ای که همان لحظه دارد سبد خریدش را باز می‌کند، پشت آن صف می‌ایستد.

### چیزهایی که **مشکل نیستند** (اندازه گرفتم، حدس نزدم)

- **فشرده‌سازی JSON:** brotli روی پاسخ ۶۶۸۶ بایتی محصولات = ۰٫۱۳۱ ms و می‌رساندش به ۱۴۸۱ بایت. ارزشش را دارد. نیازی به کش ندارد.
- **جدول‌های «بدون ایندکس»:** بیشترشان از `PRIMARY KEY` / `UNIQUE` ایندکس ضمنی دارند.
- **صفحه‌بندی سمت سرور:** از قبل هست. `main.js:418` همیشه با پارامتر صدا می‌زند.
- **Rate limit:** از قبل هست، ۳۰۰ درخواست در دقیقه روی کل `/api` به‌علاوه‌ی محدودکننده‌های اختصاصی روی ورود و آپلود.

---

## ۲. ولی صبر کن — ۱۰۰ محصول اصلاً عدد بزرگی نیست

این را صادقانه بگویم چون مهم است: در ۵۰۱۲ محصول، صفحه‌ی اول ۱٫۶۳ میلی‌ثانیه بود. در ۱۰۰ محصول حدود **۰٫۲ میلی‌ثانیه** می‌شود.

**پس اصلاحات دیتابیس برای ۱۰۰ محصول ضروری نیست. بیمه‌ی رشد است، نه اورژانس.**

آنچه در ۱۰۰ محصول **واقعاً** به دردسر می‌خورد چیز دیگری است:

1. **عکس‌ها.** ۱۰۰ عکس موبایل خام یعنی ۲۰۰ مگابایت و مرگ سایت. این تنها گلوگاه واقعی در این مقیاس است.
2. **پیدا کردن جنس.** با ۱۲ محصول، اسکرول کافی است. با ۱۰۰ محصول، مشتری بدون فیلتر گم می‌شود. این مسئله‌ی UX است، نه مسئله‌ی سرعت.
3. **۸۸ محصول بدون عکس.** تو گفتی عکس‌ها را بعداً می‌گذاری. اگر سایت زنده باشد و ۸۸ کارت خالی نشان بدهد، بدتر از نداشتن آنهاست.

---

## ۳. مقایسه با فروشگاه‌های بزرگ فارسی

> ⚠ این بخش **دانش داخلی من** است، نه منبع تازه (بخش ۰ را ببین).

الگویی که فروشگاه‌های بزرگ ایرانی در صفحه‌ی لیست محصولات دارند:

- صفحه‌بندی سمت سرور، حدود ۲۰ تا ۲۴ آیتم در هر صفحه، با **شماره‌ی صفحه** نه فقط «بیشتر»
- ستون کناری با فیلترهای شمارش‌دار: دسته، بازه‌ی قیمت، برند، «فقط موجودها»
- **همه‌ی فیلترها داخل URL** — دکمه‌ی برگشت کار می‌کند، لینک قابل اشتراک است، گوگل می‌تواند ایندکس کند
- کارت محصول با **جعبه‌ی تصویر با نسبت ثابت**، عنوان بریده در دو خط، قیمت با خط‌خوردگی قیمت قبلی
- اسکلت (skeleton) موقع بارگذاری، نه چرخ‌فلک
- تصاویر: تحویل از CDN با تغییر اندازه‌ی درجا، مذاکره‌ی webp/avif، بارگذاری تنبل زیر خط تا

### چه چیزی از این به تو می‌خورد و چه چیزی نه

| الگو | برای پلاسکو گلی؟ |
|---|---|
| فیلتر داخل URL | ✅ ارزان، برد بزرگ |
| صفحه‌بندی شماره‌دار | ✅ |
| شمارش کنار هر فیلتر | ✅ endpoint فست‌ها از قبل هست |
| جعبه‌ی تصویر با نسبت ثابت | ✅ جلوی پرش چیدمان را می‌گیرد |
| `srcset` روی تصاویر | ✅ `?w=320` و `?w=560` از قبل ساخته می‌شوند |
| اسکلت موقع بارگذاری | ✅ |
| **Elasticsearch** | ❌ SQLite تا ده‌ها هزار محصول کافی است. FTS5 هم لازم نیست. |
| **CDN** | ❌ یک مغازه در ساری، یک سرور |
| **میکروسرویس** | ❌ |

نکته‌ی مهم: **جست‌وجوی فازی‌ات از خیلی فروشگاه‌های بزرگ بهتر است.** «ستل» را پیدا می‌کند. این را دست نزن.

---

## ۴. نُه تسکی که خواستی

### بک‌اند

**B1 — کش پاسخ فهرست محصولات**
پاسخ فهرست برای همه‌ی کاربران یکسان است و فقط وقتی محصولی عوض شود تغییر می‌کند. `getCatalogSignature()` از قبل هست و دقیقاً همین را می‌داند. یک `Map` در حافظه با کلید «امضای کاتالوگ + امضای فیلتر». نتیجه: پرتکرارترین درخواست سایت از صفر کوئری جواب می‌گیرد.
*فایل:* `backend/routes/products.js`

**B2 — اصلاح شش زیرکوئری `date()`دار در `stmtStats`**
جایگزینی با مقایسه‌ی بازه‌ای روی `created_at` خام. مرزهای روز/هفته/ماه در جاوااسکریپت حساب می‌شوند و به‌عنوان پارامتر می‌روند.
اندازه‌گیری‌شده: ۲۶٫۴۰ ms → انتظار زیر ۱ ms.
*فایل:* `backend/lib/db.js:990-1012`

**B3 — مهار دو کوئری بی‌مرز ادمین**
`getTopProducts` روی **هر** سفارش پرداخت‌شده‌ی تاریخ `json_each` می‌زند بدون هیچ محدوده‌ی زمانی → پنجره‌ی ۹۰ روزه.
`getProductsWithSales` کل جدول محصولات را می‌کشد داخل جاوااسکریپت → صفحه‌بندی سمت سرور.
اندازه‌گیری‌شده: ۲۷٫۵۵ ms → هدف زیر ۳ ms.
*فایل:* `backend/lib/db.js:1110-1124` و `getProductsWithSales`

### امنیت

**S1 — سخت‌کردن مسیر آپلود عکس** ⭐ *مهم‌ترین تسک امنیتی این مرحله*
تو قرار است ۱۰۰ عکس موبایل آپلود کنی. این لحظه‌ای است که مسیر آپلود بیشترین فشار را می‌بیند. لازم است:
- **پاک‌کردن EXIF.** عکس موبایل **مختصات GPS** دارد. اگر عکس‌ها را در مغازه یا خانه بگیری، آدرس دقیقت داخل فایل می‌رود روی اینترنت. این جدی‌ترین مورد کل گزارش است.
- تشخیص نوع فایل از **بایت‌های ابتدایی**، نه از پسوند
- رد کردن کامل SVG (SVG می‌تواند اسکریپت حمل کند)
- سقف ابعاد و حجم قبل از پردازش
- پاک‌سازی نام فایل

**S2 — سقف صفحه‌بندی عمیق**
الان `page` تا ۱۰۰۰۰ و `limit` تا ۶۰ مجاز است، یعنی کسی می‌تواند `OFFSET 600000` بخواهد. روی موتور همگام، این یعنی قفل‌شدن سرور با یک درخواست. باید صفحه‌ی خارج از محدوده ۴۰۰ بگیرد، نه اینکه اسکن شود.
*فایل:* `backend/routes/products.js`

**S3 — سطل جدا برای جست‌وجو**
Rate limit عمومی (۳۰۰ در دقیقه) هست و منطقی است، ولی جست‌وجوی فازی تنها مسیر عمومی است که می‌تواند اسکن کامل جدول بزند. یک سطل کوچک‌تر فقط وقتی `q` وجود دارد. دفاع لایه‌ای، تغییر کوچک.

### فرانت‌اند

**F1 — صفحه‌ی «همه‌ی محصولات»** (`products.html`) — جزئیات در بخش ۵

**F2 — انضباط تصویر و جانشین آبرومند**
- جعبه‌ی نسبت ثابت با `aspect-ratio` + `width`/`height` صریح → پرش چیدمان صفر
- `srcset` از `?w=320` و `?w=560` که از قبل ساخته می‌شوند
- `loading="lazy"` روی همه به‌جز ردیف اول، `fetchpriority="high"` روی تصویر LCP
- کارت اسکلتی موقع بارگذاری
- **جانشین برای محصولات بدون عکس:** ستون `icon` از قبل هست (`i-bucket`, `i-package`). همان آیکون در جعبه‌ی هم‌اندازه، با پس‌زمینه‌ی ملایم. با ۸۸ محصول بی‌عکس، این تفاوت «سایت ناقص» و «سایت مرتب» است.

**F3 — کشف محصول**
با ۱۰۰ محصول، مشکل واقعی «پیدا کردن» است نه «نمایش دادن». نوار دسته‌بندی در صفحه‌ی اصلی که به URLهای از پیش فیلترشده لینک می‌دهد، لینک در هدر، و مسیر راهنما (breadcrumb) در صفحه‌ی لیست.

---

## ۵. صفحه‌ی «همه‌ی محصولات» — طرح پیشنهادی

```
┌──────────────────────────────────────────────────┐
│  خانه ‹ همه‌ی محصولات ‹ ظروف نگهداری             │  ← مسیر راهنما
├──────────────┬───────────────────────────────────┤
│ دسته‌بندی    │  ۱۰۰ محصول    [مرتب‌سازی ▾]      │
│ ▸ همه (۱۰۰) │                                   │
│ ▸ ظروف (۲۴) │  ┌────┐ ┌────┐ ┌────┐ ┌────┐    │
│ ▸ سبد  (۱۸) │  │    │ │    │ │    │ │    │    │
│              │  └────┘ └────┘ └────┘ └────┘    │
│ بازه‌ی قیمت │  ┌────┐ ┌────┐ ┌────┐ ┌────┐    │
│ [__] تا [__] │  │    │ │    │ │    │ │    │    │
│              │  └────┘ └────┘ └────┘ └────┘    │
│ ☑ فقط موجود │                                   │
│              │      ‹ ۱ [۲] ۳ ۴ ... ۵ ›         │
│  [پاک کردن]  │                                   │
└──────────────┴───────────────────────────────────┘
```

- **۲۴ محصول در هر صفحه** (۴ ستون × ۶ ردیف در دسکتاپ، ۲ ستون در موبایل)
- **URL همه‌چیز را نگه می‌دارد:** `products.html?category=ظروف+نگهداری&maxPrice=300000&page=2`
  → برگشت مرورگر کار می‌کند، لینک قابل اشتراک است، گوگل ایندکس می‌کند
- **در موبایل** ستون کناری تبدیل می‌شود به دکمه‌ی «فیلتر» که از پایین باز می‌شود
- **صفحه‌ی اصلی دست‌نخورده می‌ماند** — فقط یک لینک به این صفحه اضافه می‌شود

API از قبل همه‌ی این را دارد: `category`, `minPrice`, `maxPrice`, `inStock`, `sort`, `page`, `limit` و endpoint فست‌ها برای شمارش‌ها. **هیچ endpoint جدیدی لازم نیست.**

---

## ۶. عکس‌ها — مشخصات دقیق

وقتی می‌خواهی عکس بگیری:

| مورد | مقدار |
|---|---|
| قاب | مربع، محصول حدود ۸۰٪ کادر |
| پس‌زمینه | سفید یا روشن یکدست |
| اندازه‌ی ذخیره | ۱۲۰۰×۱۲۰۰ |
| فرمت | webp با کیفیت ۸۰ → حدود ۱۰۰ کیلوبایت |
| بندانگشتی | `?w=320` و `?w=560` خودکار ساخته می‌شوند |

۱۰۰ محصول × (اصلی + دو بندانگشتی) ≈ **۲۰ مگابایت** روی دیسک. هیچ مشکلی نیست.

---

## ۷. مسئله‌ی ۸۸ محصول بدون عکس

اگر ۱۰۰ محصول ساختگی را مستقیم داخل دیتابیس زنده بریزم، مشتری می‌تواند چیزی سفارش بدهد که وجود ندارد. دو راه:

**الف) ستون `published`** (پیشنهاد من)
یک ستون، پیش‌فرض `0` برای محصولات تازه‌ساخته، یک `WHERE` در API عمومی. در پنل ادمین می‌بینی‌شان، مشتری نه. هر کدام را که عکس و قیمت واقعی گرفت، منتشر می‌کنی. ابزاری است که برای همیشه به کارت می‌آید.

**ب) موجودی صفر**
ساده‌تر، بدون تغییر ساختار، ولی سایت ۸۸ محصول «ناموجود» نشان می‌دهد که ظاهر خوبی ندارد.

همه‌ی محصولات ساخته‌شده یک نشانه می‌گیرند تا اگر پشیمان شدی با **یک کوئری** همه پاک شوند.

---

## ۸. ترتیب اجرا

۱. **پشتیبان‌گیری** (طبق قرارداد پروژه)
۲. ستون `published` + اسکریپت ساخت ۱۰۰ محصول واقع‌گرایانه
۳. B1 (کش کاتالوگ) — بیشترین اثر برای هدف ۱۰۰ محصول
۴. S1 (سخت‌کردن آپلود) — **قبل از اینکه عکس‌ها را آپلود کنی**
۵. S2 و S3
۶. F1 و F2 و F3
۷. B2 و B3 (بیمه‌ی رشد، اورژانس نیست)
۸. بالا بردن `?v=` در همه‌ی فایل‌های HTML
۹. اجرای هر ۶۹۶ تست
۱۰. به‌روزرسانی `یادداشت-کلود.md` و کامیت

اگر عجله داری، مراحل ۲ و ۴ و ۶ حداقلِ کاری است که هدف «۱۰۰ محصول» را برآورده می‌کند. بقیه می‌تواند بماند.

---

## ۹. جمع‌بندی صادقانه

- سایتت در ۱۰۰ محصول **کند نخواهد بود**. اصلاحات دیتابیس بیمه‌ی رشد است.
- خطر واقعی این مرحله **عکس‌ها** است — هم از نظر حجم، هم **مختصات GPS داخل EXIF**.
- بیشترِ کاری که لازم است، **UX** است نه مهندسی: راهی برای پیدا کردن جنس بین ۱۰۰ تا.
- زیرساخت از آنچه فکر می‌کردم بهتر است. صفحه‌بندی، فست‌ها، جست‌وجوی فازی، بندانگشتی‌ها، فشرده‌سازی، ETag — همه از قبل هست.
