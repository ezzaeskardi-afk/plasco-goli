# گزارش بنچمارک بار — فروشگاه پلاسکو گلی

> این فایل روندِ کارایی سایت را نگه می‌دارد تا هر اجرا با اجرای قبلی قابل مقایسه باشد.
> هر بار که بنچمارک می‌گیری، **بلوک جدیدی پایین همین جدول‌ها اضافه کن** — بلوک قبلی را حذف نکن.

## مشخصات اجرا

| مورد | مقدار |
|---|---|
| تاریخ/زمان (UTC) | 2026-08-22T12:00:12Z |
| نسخه‌ی Node | v24.18.0 |
| سیستم‌عامل | win32 x64 |
| تعداد هسته‌ها | 16 |
| کامیت (HEAD) | `0dd8a0d` — هماهنگ‌سازی کامل پالت رنگ تم تیره بین همهٔ صفحات |
| فرمان | `node bench-load.js 400 3996 0 0` |
| حالت | تست ایزوله (PG_DATA_DIR موقت — دیتابیس واقعی دست‌نخورده) |

## سناریو

400 خریدار هم‌زمان از یک IP مشترک (نمای CGNAT — بدترین حالت واقعی): هر کدام سبد می‌گیرد و سفارش ثبت می‌کند.

## نتایج

### GET /api/cart (هم‌زمان)

| متریک | مقدار |
|---|---|
| موفقیت | 400 / 400 (100.0%) |
| شکست | 0 |
| تأخیر p50 | 125 ms |
| تأخیر p90 | 167 ms |
| تأخیر p95 | 172 ms |
| تأخیر p99 | 174 ms |
| حداکثر | 175 ms |

### POST /api/orders (هم‌زمان)

| متریک | مقدار |
|---|---|
| موفقیت | 400 / 400 (100.0%) |
| شکست | 0 |
| تأخیر p50 | 145 ms |
| تأخیر p90 | 230 ms |
| تأخیر p95 | 238 ms |
| تأخیر p99 | 251 ms |
| حداکثر | 253 ms |
| زمان کل | 277 ms |
| توان عملیاتی | **1444.0 req/s** |

### سلامت پس از بار

| متریک | مقدار |
|---|---|
| دیتابیس | ok=true، پاسخ 0.06 ms |
| حافظه (RSS) | 94 MB |
| هیپ | 12 MB |

## جمع‌بندی

- ✅ هر 400 خریدار موفق — هیچ ستاره‌ای گرسنه نماند (rate-limit مشترک IP صحیح)
- ✅ ~1444 سفارش/ثانیه — برای فروشگاه محلی چند برابر سقف نیاز است
- ✅ دیتابیس بعد از بار سالم است (SQLite با WAL + کش 64MB)

---

<!-- ===================== تاریخچه (قدیمی‌ترین در پایین) ===================== -->
