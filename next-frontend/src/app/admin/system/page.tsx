import type { Metadata } from "next";
import { SystemContent } from "@/components/admin/SystemContent";

// ============================================================
// `/admin/system` — وضعیت سیستم
// ============================================================
// همان نمای «وضعیت سیستم» در پنل Express (`data-view="system"`): متریک سرور،
// سلامت دیتابیس، فهرست بکاپ‌ها با دکمه‌ی «بکاپ فوری»، خطاهای هفت روزِ اخیر و
// رویدادهای اخیرِ پنل.
//
// چرا این نما جای خودش را دارد و در «تنظیمات» ادغام نشد: بقیه‌ی نماها درباره‌ی
// کارِ فروشگاه‌اند (سفارش، کالا، پول) و این یکی درباره‌ی خودِ سرور. تفاوت در
// کاربردشان است، نه در داده: اینجا مدیر می‌آید تا بفهمد «مشکلی هست یا نه»،
// پس پاسخ قبل از عدد می‌آید.
//
// بکاپِ دستی و خروجیِ اکسل که در کارتِ «نگهداری»ِ Express زیرِ تنظیمات بودند،
// اینجا هستند (بکاپ) یا هنوز منتقل نشده‌اند (خروجیِ اکسل). «تنظیم» نیستند،
// عمل‌اند.
//
// `robots` عمداً نیست — پوسته‌ی `/admin` آن را می‌دهد.

export const metadata: Metadata = {
  title: "وضعیت سیستم",
};

export default function AdminSystemPage() {
  return (
    <div className="mx-auto max-w-[1180px] px-4 py-6 sm:px-6 sm:py-8">
      <h1 className="text-2xl font-extrabold text-ink mb-2">وضعیت سیستم</h1>
      <p className="text-xs mb-6" style={{ color: "var(--color-ink-dim)" }}>
        سلامت سرور و دیتابیس، بکاپ‌ها و خطاهای هفت روزِ اخیر. این صفحه هر دقیقه
        خودش تازه می‌شود.
      </p>
      <SystemContent />
    </div>
  );
}
