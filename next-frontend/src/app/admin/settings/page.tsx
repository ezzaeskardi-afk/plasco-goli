import type { Metadata } from "next";
import { SettingsContent } from "@/components/admin/SettingsContent";

// ============================================================
// `/admin/settings` — تنظیمات فروشگاه
// ============================================================
// همان نمای «تنظیمات» در پنل Express (`data-view="config"`): نام و تماس و آدرسِ
// فروشگاه، هزینه‌ی ارسال و آستانه‌ی ارسالِ رایگان، پیام اطلاعیه، بنر جشنواره و
// سوئیچِ باز/بسته بودنِ فروشگاه.
//
// این نما آخرین جایی است که در پنل Express می‌شد فروشگاه را بست یا هزینه‌ی
// ارسال را عوض کرد. تا وقتی اینجا نبود، مدیرِ روی پنلِ جدید برای یک تغییرِ
// یک‌خطی باید به پنلِ قدیمی برمی‌گشت — و دو پنل روی یک جدول یعنی فرصتِ
// ناهماهنگی.
//
// بکاپِ دستی و خروجیِ اکسل که در همان کارتِ Express زیرِ «نگهداری» بودند اینجا
// نیستند: آن دو «تنظیم» نیستند، عمل‌اند. جایشان نمای «وضعیت سیستم» است.
//
// `robots` عمداً نیست — پوسته‌ی `/admin` آن را می‌دهد.

export const metadata: Metadata = {
  title: "تنظیمات",
};

export default function AdminSettingsPage() {
  return (
    <div className="mx-auto max-w-[1180px] px-6 py-8">
      <h1 className="text-2xl font-extrabold text-ink mb-2">تنظیمات فروشگاه</h1>
      <p className="text-xs mb-6" style={{ color: "var(--color-ink-dim)" }}>
        این مقادیر در دیتابیس ذخیره می‌شوند و بدونِ دست زدن به کد قابل تغییرند.
      </p>
      <SettingsContent />
    </div>
  );
}
